services:
  typescript-app:
    build:
      context: .
    container_name: typescript-app
    restart: unless-stopped
    init: true
    ports:
      - "4200:4200"  # Angular dev server / static server
    # env_file: ./.env  # Uncomment if .env file exists
    # No external dependencies detected in README or source
    # If you add a database or other service, add it here
    # networks can be added if more services are introduced

# No external services (databases, caches, etc.) detected in README or source
# No volumes section needed as there are no persistent data services
