services:
  typescript-app:
    build:
      context: .
    container_name: typescript-app
    restart: unless-stopped
    init: true
    # env_file: ./.env  # Uncomment if .env file exists
    ports:
      - "4000:4000"  # Exposes the SSR server as per Dockerfile and README
    # No external dependencies detected (e.g., database, cache)
    # Runs as non-root user as per Dockerfile
    # No volumes needed as this is not a database or persistent service
    # If you need to provide environment variables, create a .env file and uncomment the env_file line

# No external services (databases, caches, etc.) detected in the documentation or code
# No volumes section needed as there are no persistent data requirements
# No custom networks needed as there is only one service
